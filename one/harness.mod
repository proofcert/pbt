module harness.

% A good discipline is to put all accumulate-keywords in this harness
% file.  This will make sure that we do not have repeated code loaded.

accumulate infra.
accumulate counter, cr, lists.
