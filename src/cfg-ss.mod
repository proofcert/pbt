module cfg-ss.

prog (ss L)
     (or (and (eq L null)
              tt
         )
     (or (and (eq L (cons b W))
              (aa W)
         )
         (and (eq L (cons a W))
              (bb W)
         )
     )).
